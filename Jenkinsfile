pipeline {
    agent none
    stages {
        stage("Run Unit Tests") {

        }
        stage("Deploy CloudFormation Stack") {
            steps {
                sh ''
            }
        }
    }  
    post {
        always {
            sh
        }
    }
}